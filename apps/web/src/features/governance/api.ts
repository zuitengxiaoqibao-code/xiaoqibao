import type { AuditStatus, ComplianceStatus, RiskStatus } from "./GovernanceViews";
async function read<T>(url: string): Promise<T> { const response = await fetch(url); if (!response.ok) throw new Error(response.status === 503 ? "审计仓暂不可用" : "治理服务请求失败"); return response.json(); }
export const loadRisk = () => read<RiskStatus>("/api/v1/xingbu/status"); export const loadCompliance = () => read<ComplianceStatus>("/api/v1/libu/status"); export const loadAudit = () => read<AuditStatus>("/api/v1/dongchang/findings");
export async function complianceAction(source: string, action: string): Promise<Record<string, unknown>> { const response = await fetch(`/api/v1/libu/sources/${source}/${action}`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ permission_reference: "operator:ui", disclaimer_version: "2026-07" }) }); if (!response.ok) throw new Error("合规操作失败"); return response.json(); }
